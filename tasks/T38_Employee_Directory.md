Task ID: T38

Section: Admin & Setup

Component: Employee Directory

Category: [BACKEND]

Priority: High

Est. Time: 3h

Description

Create/migrate `audit_logs` table and guard empty state (hide tab if disabled)
Context: Audit tab shows error "public.audit_logs does not exist"

Dependencies

T06

Expected Outcome

Once complete, Employee Directory will:
- Create/migrate `audit_logs` table and guard empty state (hide tab if disabled)

Technical Scope

- **Affected Files:** Codex will identify automatically from repo.
- **APIs / DB:** Create/migrate `audit_logs` table and guard empty state (hide tab if disabled)
- **UI Components:** None noted.
- **Co-Pilot Links:** None.
